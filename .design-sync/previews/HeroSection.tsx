import { HeroSection } from 'roboinvestor-app'

/** The full landing hero: gradient headline, value proposition, and the three
 *  feature tiles (portfolio tracking, filings research, AI insights). Full-bleed
 *  on a black canvas — give the card a tall viewport so the section fills. */
export const Default = () => <HeroSection />
